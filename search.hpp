#ifndef SEARCH_HPP
#define SEARCH_HPP

#include <bits/stdc++.h>
#include"dict.hpp"
using namespace std;

vector<string> get_suggestions(const vector<string>& words, const string& prefix, int max_suggestions);

#endif
